print("""
Register
Login

Register
    generate Id, log off
    if ticket for type_ticket:
        sugguest other
            if not want other
            sorry. bye.
    Give ticket
    If NO MORE TICKET:
        process all.

Login.
    Cancel
    downgrade/upgrade if this else downgrde

    if vip full - can't do

""")